<%= form_for @book do |f| %>

  <p>Title:</p>
    <%= f.text_field(:title)%>
  <p>Author</p>
  <%= f.text_field(:author)%>
  <p>Published:</p>
  <%= f.date_select(:released_on)%>
  <%= f.submit('Save') %>

<% end %>
