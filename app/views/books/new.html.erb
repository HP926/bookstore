<%= form_for @book do |f| %>

  <p>Title:</p>
    <%= f.text_field(:title)%>
  <p>Author</p>
  <%= f.text_field(:author)%>
  <p>Published:</p>
  <%= f.select(:released_date, 1949..2015)%>
  <%= f.submit('Save') %>

<% end %>
